{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "npm",
			"script": "build",
			"group": "build",
			"problemMatcher": [],
			"label": "npm: build",
			"detail": "tsc -p tsconfig.prod.json && cp -rf src/private dist/ && mkdir -p dist/src/ && cp -rf src/initialData dist/src/"
		}
	]
}